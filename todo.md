# TODO

本文档汇总当前项目梳理与 `block`/`inline` 布局审查中发现的问题。条目按类型归类；`P0` 表示会破坏布局正确性的确定性缺陷，`P1` 表示重要能力或稳定性问题，`P2` 表示增强项。

最后核对：2026-08-31（`cf199a4`）。`[x]` 表示已经由当前实现和测试/文档确认完成；标有“部分完成”的项目仍保持未勾选。

## 最近完成与进展

- 已修复 Inline 子元素高于容器时无法真正垂直居中的问题，并增加超大内容居中测试。
- 已重写 `Async`/`Dirty` 唤醒机制：不再为每次投递创建 goroutine，唤醒信号会合并；pending 回调队列仍无上限。
- 已增加并持续维护中文 README，明确 Go 1.27、`GOEXPERIMENT=simd`、平台要求和项目边界。
- 百分比高度、四边 padding、SafeArea、文本换行/滚动、Scroll 固定/最大行数及固定行高已经加入，但不等于完成下列更通用的布局任务。
- CSS 已支持嵌套规则，并在 README 中记录当前选择器、层叠、继承和限制。
- 2026-09-03：已修复常规布局的百分比重排和根元素百分比；负 Spacer 和负 Inline 宽度尚未处理。
- 2026-09-03：新增单行 Flex 内部布局，支持 row/column、gap、grow、主轴/交叉轴对齐及 align-self；父布局通过固定尺寸约束分配空间，不修改样式。

## 1. 确定性布局 Bug

- [x] **P0：百分比宽度不得在布局时被永久改写为像素值**
  - 2026-09-03：移除 `presetSize`，通过 `Constraints` 传入完整父内容区尺寸，局部解析宽高，不修改 `computedStyles`。
  - 已测试父尺寸扩大、缩小、重复布局、前序兄弟元素和 padding/border。

- [ ] **P0：实现百分比高度**
  - **部分完成：** 常规 `Calc` 路径已无副作用地解析百分比高度，父尺寸变化后会重新计算。
  - 文本的独立分段路径尚未统一接入；内容自适应父容器的百分比参考尺寸仍需明确。

- [x] **P0：支持根元素的百分比尺寸**
  - 2026-09-03：根元素以文档尺寸作为百分比参考，与子元素共用长度解析流程，并覆盖嵌套百分比重排测试。

- [ ] **P0：禁止 Block Spacer 得到负高度**
  - 固定内容超过容器高度时，剩余空间会是负数，当前会直接分配给 Spacer。
  - Spacer 最小尺寸应为 0；溢出应由明确的 overflow 策略处理。

- [ ] **P0：禁止 Inline 子元素和 Spacer 得到负宽度**
  - 前序内容超过可用宽度后，负的剩余宽度会继续传给后续子元素。
  - 所有传入子布局的可用尺寸以及最终布局尺寸都不得小于 0。

- [ ] **P0：内容区尺寸不得因 padding/border 变成负数**
  - 当指定宽高小于两侧 padding 与 border 之和时，`contentAvailWidth/Height` 当前会变成负数。
  - 内容区应至少钳制为 0，同时保留明确的盒模型溢出规则。

- [x] **P1：统一 Block 与 Inline 的溢出居中行为**
  - Inline 已改为基于最终内容区高度计算偏移，超高子元素会向上下等量溢出。
  - `TestButtonCentersOversizedContent` 和文本高度相关测试覆盖了该行为。

- [ ] **P2：处理 Spacer 平均分配的整数余数**
  - 剩余空间不能整除 Spacer 数量时，目前会遗失 1～N 个像素。
  - 将余数稳定地分配给前几个或后几个 Spacer，保证总尺寸精确守恒。

## 2. 布局尺寸模型与架构

- [ ] **P0：建立统一、无副作用的长度解析函数**
  - **部分完成：** `resolveLayoutLength` / `resolveDimensions` 已处理像素、百分比和未指定值，宽高使用相同机制，不再由父容器预先改写样式。
  - 文本分段路径仍待统一接入。

- [ ] **P1：拆分测量与最终布局阶段**
  - 当前 `Calc` 同时负责内容测量、尺寸解析、剩余空间分配和坐标写入，复杂布局会产生循环依赖和特殊分支。
  - 建议形成 `Measure(constraints) -> desiredSize` 与 `Layout(finalRect)` 两阶段。

- [ ] **P1：用完整尺寸约束替代两个裸可用尺寸和布尔偏好**
  - 约束至少应能表达 `MinWidth/MaxWidth/MinHeight/MaxHeight`。
  - 区分固定尺寸、最大可用尺寸和无界内容测量。

- [ ] **P1：定义统一的溢出策略**
  - 明确固定子元素超过父容器时，父尺寸、子坐标、绘制和事件命中应如何处理。
  - 不得依赖 Canvas 碰巧忽略负尺寸来掩盖布局错误。

- [ ] **P1：支持内容驱动和跨兄弟元素的尺寸计算**
  - 覆盖父尺寸由子内容决定、同行统一高度、表格式列宽由多行共同决定等需求。

## 3. Block / Inline 基础布局能力

- [ ] **P1：为 Block 和 Inline 增加统一 `gap`**
  - 支持只在子元素之间留间距，不在首尾产生额外空间。
  - 可进一步区分 `row-gap` 和 `column-gap`。

- [ ] **P1：补充完整的主轴与交叉轴对齐**
  - 支持 start、center、end。
  - 支持 space-between、space-around、space-evenly。
  - 分离水平与垂直对齐属性，不再只依赖 `align` 的四个值。

- [ ] **P1：支持子元素独立对齐**
  - 提供类似 `align-self` 的能力。

- [ ] **P1：支持文本和图标的 baseline 对齐**
  - 同行不同字号的文字或图标不应只能使用顶部或几何居中。

- [ ] **P1：实现 Inline 自动换行和行模型**
  - 维护每一行的宽度、高度和对齐。
  - 支持标签列表、按钮组和宽度不固定的横向内容。

- [ ] **P2：增加通用 margin 或明确的间距替代方案**
  - 覆盖元素独立四方向间距、首尾间距和相邻元素间距。
  - 不要求实现浏览器的 margin collapse，除非确有需求。

## 4. Flex 与 Grid 类需求

- [ ] **P1：实现适合项目范围的轻量 Flex 分配**
  - **部分完成：** `<flex>` 已支持 row/column、不同权重的正剩余空间分配、整数余数、gap、justify-content、align-items、align-self，并覆盖嵌套、重排和文本断行测试。`display` 现为仅控制显示/隐藏的 bool，不能切换布局类型。
  - 支持空间不足时的 shrink 和最小尺寸约束。
  - 视需求支持 basis、反向排列和顺序调整。

- [ ] **P2：评估并实现通用 Grid，而不是复用 Scroll 充当 Grid**
  - 支持不等宽列、固定列加弹性列、内容决定列宽。
  - 视需求支持跨行、跨列、自动列数和统一行高。

## 5. 尺寸约束与响应式能力

- [ ] **P1：支持 min/max width/height**
  - 覆盖内容自适应但不超过容器、按钮最小尺寸等需求。

- [ ] **P1：支持图片或容器的宽高比约束**
  - 只指定宽或高时可保持 `aspect-ratio`。

- [ ] **P2：增加响应式布局机制**
  - 支持根据屏幕宽高或横竖屏切换布局。
  - 支持条件显示、自动改变列数和安全区域。
  - **部分完成：** 已实现系统 Overlay 与 `<safe-area>`，覆盖层尺寸变化时会触发普通文档重排；横竖屏条件布局和自动列数仍未实现。

## 6. 定位、叠层、裁剪与滚动

- [ ] **P1：扩展 Stack 或新增定位布局**
  - 支持 top/right/bottom/left 锚定、绝对定位和边缘定位。
  - 支持明确的层叠顺序，而不是只依赖子元素顺序。

- [ ] **P1：实现通用裁剪区域和 overflow**
  - 至少支持 `overflow: hidden`。
  - 绘制、图片、文字和事件命中均应遵守裁剪区域。

- [ ] **P1：实现通用 Viewport/滚动容器**
  - 支持任意内容的横向或纵向滚动、滚动偏移和可视区域。
  - 不应把固定槽位的 `Scroll` 当作所有滚动需求的替代品。

- [ ] **P2：支持可变高度虚拟列表**
  - 支持内容决定项高度、分组标题、展开项和吸顶标题。
  - 维护每项尺寸或累计偏移，并据此计算可见范围。

## 7. 文本布局

- [ ] **P1：实现真正的多行图文 Inline 排版**
  - 支持多个文本 run、粗体/斜体片段和图片在行内共同换行。
  - **部分完成：** `<text>` 已支持多 run、多行、显式换行符、逐行/逐页滚动及每行水平居中；图片仍不能加入文字行，容器级 Inline 也不会自动换行。

- [ ] **P1：支持行高、最大行数、截断和省略号**

- [ ] **P2：改进断行规则**
  - 支持单词边界、中文断行和复杂语言规则。
  - 视产品需求评估双向文字、字间距和段间距。

## 8. Scroll 组件限制

- [ ] **P1：明确 Scroll 只支持固定行列、固定槽位尺寸的约束**
  - 对无效的 rows、cols、count 和索引进行校验，避免除零或越界。
  - **部分完成：** README 已明确固定槽位模型，并记录 `rows`、`max-rows`、`row-height` 与 `gap` 的组合语义；参数合法性校验仍不足。

- [ ] **P1：支持不同尺寸列表项或提供独立组件**
  - 当前 `rows × cols` 均分视口，无法覆盖新闻流、说明文字、分组或瀑布流。

- [ ] **P2：按需求增加鼠标、触摸、滚轮、惯性滚动和滚动条**

## 9. DOM、HTML 与 CSS 子集

- [ ] **P1：解决自定义标签自闭合解析问题**
  - 标准 HTML parser 会错误处理 `<spacer/>` 等自定义自闭合标签。
  - 可改用 tokenizer、自有解析器，或严格禁止并在加载时给出清晰错误。

- [x] **P1：为受支持的 HTML/CSS 子集编写正式规范**
  - README 已记录文档结构、内置标签、样式属性、选择器、CSS 嵌套、specificity、层叠顺序、继承、布局约束和已知限制。
  - 新增语法或组件时继续同步维护 README。

- [ ] **P2：评估是否需要属性选择器、伪类等更多选择器能力**
  - 不以实现完整 CSS 为目标，只按真实业务需求扩展。

## 10. 渲染与刷新性能

- [ ] **P1：避免每次有效刷新清屏并重绘全部可见文档**
  - 评估脏矩形、分层缓存或局部重绘。
  - **部分完成：** 后台 Desktop 的脏文档不再触发前台刷新；同一前台栈内仍以整帧清屏和重绘为主。

- [ ] **P1：评估 framebuffer 整帧复制成本和真正双缓冲方案**
  - 当前同步不是原子双缓冲，可能出现撕裂或无效带宽消耗。

- [ ] **P1：控制 Async goroutine 和事件队列增长**
  - `Async` 现在把回调追加到受锁保护的 pending 队列，不再为每次投递创建 goroutine。
  - 容量为 1 的 `unblock` 通道会合并重复唤醒；`Dirty` 直接复用该机制并保持非阻塞。
  - **部分完成：** pending 回调切片仍然无界；生产速度持续高于主线程消费速度时仍会增长，需要背压、上限或可合并任务策略。

- [ ] **P2：为布局增加缓存或跳过尺寸未变化的重复计算**

## 11. 并发与错误处理

- [ ] **P1：明确 UI 树、样式和资源管理器的线程模型**
  - 写操作应限制在主线程，或提供明确同步保护。

- [ ] **P1：减少公共路径中的 panic**
  - 文档加载、无效属性、字体缺失和组件参数错误应尽可能返回带上下文的错误。

- [ ] **P1：处理异步图片加载的生命周期**
  - Document/App 关闭后，异步回调不应继续访问已释放状态。

## 12. Linux 与平台后端

- [ ] **P0：移除 Linux 输入设备 `matches[3]` 硬编码**
  - 少于四个设备时会越界，设备枚举顺序变化时也可能选错。
  - 应根据设备能力或名称识别目标控制器。

- [ ] **P1：完整处理 framebuffer ioctl、mmap、stride 和 bpp 错误**
  - 不应忽略 ioctl 返回值或假定所有设备都是相同像素格式。

- [ ] **P1：定义 Detach/Attach 对 framebuffer 和输入设备的真实所有权行为**

- [ ] **P2：决定是否支持 Windows；若不支持，在文档和构建阶段明确说明**

## 13. 文档、示例与工程配置

- [x] **P1：增加 README**
  - 已覆盖项目定位、支持平台、依赖、构建方式、文档语法、布局、组件、事件、示例、测试和已知限制。

- [ ] **P1：修复 Demo 字体依赖**
  - `demo` 引用的 `regular.ttf` 当前不在仓库中。
  - 提供可用字体、下载说明，或允许明确配置系统字体路径。
  - **部分完成：** 本地已有 `demo/fonts` 和 `demo/regular.ttf` 软链接，但两者被 `.gitignore` 排除，克隆仓库后仍无法直接运行 Demo。

- [x] **P1：确认并说明 Go 工具链要求**
  - 项目已升级到 Go 1.27；README 明确说明构建和测试还需要 `GOEXPERIMENT=simd`。

- [ ] **P2：为公共 API 增加 GoDoc 和稳定性说明**

## 14. 测试缺口

- [ ] **P0：增加布局非负尺寸不变量测试**
  - 任意 Box 的最终宽高不得小于 0。

- [ ] **P0：增加百分比重复布局测试**
  - 父尺寸、padding、class 或屏幕尺寸变化后必须重新正确计算。

- [ ] **P0：增加根元素百分比和百分比高度测试**

- [ ] **P0：增加内容溢出与 Spacer 测试**
  - 覆盖固定内容超过容器、padding 大于尺寸、Inline 剩余空间为负等情况。

- [ ] **P1：增加 Block/Inline 对齐一致性测试**
  - **部分完成：** 已增加 Inline/Button 超大内容居中、文本行水平居中和低高度文本测试。
  - 仍需覆盖 Block 与 Inline 的成对一致性、超宽内容、四边 padding、border 和多个子元素。

- [ ] **P1：增加尺寸守恒测试**
  - Spacer、gap 和余数分配后，各项尺寸总和应与容器内容区一致。

- [ ] **P1：增加 framebuffer、SDL 和完整事件循环集成测试**

- [ ] **P1：增加异步图片加载、缓存、关闭竞态和错误路径测试**

- [ ] **P1：增加 fuzz/property 测试**
  - 随机生成尺寸、padding、border、Spacer 和嵌套结构。
  - 验证不 panic、不出现负尺寸，并保持基本布局不变量。

## 15. 项目范围决策

- [x] **P1：明确项目不是完整浏览器布局引擎**
  - README 已将项目定位为固定屏幕、方向键操作设备的轻量 GUI，并明确不应视为完整浏览器布局引擎或通用桌面 GUI。

- [ ] **P1：确定短期布局能力边界**
  - 建议优先完成：百分比与负尺寸修复、gap、完整对齐、min/max、测量/布局拆分、裁剪 Viewport。
  - Grid、margin collapse、完整 CSS 定位和复杂排版仅在真实产品需求出现时实现。
